# Cula: Веб-приложение для выполнения базовых математических вычислений

**Cula** — это веб-приложение, разработанное на Flask (Python), которое позволяет выполнять базовые математические вычисления. Оно предоставляет простой и удобный интерфейс для пользователя.

---

## Описание

Калькулятор поддерживает следующие функции:

- **Сложение** (`+`)
- **Вычитание** (`-`)
- **Умножение** (`*`)
- **Деление** (`/`)
- **Возведение в степень** (`x^y`)
- **Вычисление процентов** (`%`)
- **Очистка экрана** (`C`)

Все выражения вычисляются на серверной стороне с использованием Flask. Результаты отображаются на экране в реальном времени.

## Скриншоты

### Главный экран
![Главный экран](screenshots/screenshot1.png)

---

## Установка

### Требования
- Python 3.7 или выше
- Flask библиотека

### Шаги запуска:
1. Скачайте проект.
2. Перейдите в папку проекта через терминал или редактор (например, PyCharm).
3. Запустите сервер, выполнив следующую команду:
   ```bash
   python app.py
4. После запуска перейдите по ссылке http://127.0.0.1:5000 в вашем браузере.

## Пример использования

1. Откройте приложение в браузере.
2. Используйте кнопки калькулятора для ввода математического выражения.
3. Нажмите `=` для вычисления результата.
4. Результат будет отображён на экране.

---

## Основные файлы

### `app.py`
Это основной файл приложения, который реализует серверную логику с использованием Flask. Он содержит два маршрута:

- `/` — отображает пользовательский интерфейс калькулятора.
- `/calculate` (POST) — получает математическое выражение от клиента, вычисляет его и возвращает результат.

### `calculator.html`
HTML-файл, который определяет пользовательский интерфейс калькулятора. Он включает в себя кнопки для ввода чисел и операций, а также поле для отображения результатов.

### `style.css`
CSS-файл, отвечающий за стилизацию калькулятора. Вы можете изменить цвет кнопок, фон и другие стили в этом файле.


